# GameSheet Assistant 4.2

- Overtime-tie warnings now report the actual final score and clarify that only the overtime period was scoreless.
- Team matching now ignores the standalone word `Area`, so names such as `Alexandria` and `Alexandria Area` are treated as the same program.
- Updated GameSheet Web Fill helper to version 0.9.4.
